# Shared by `make lint` and CI, so they cannot drift apart.
extends: default

rules:
  line-length:
    max: 120
  document-start: disable
  truthy:
    # GitHub Actions' `on:` key is not a boolean, whatever YAML 1.1 thinks.
    check-keys: false
  comments:
    min-spaces-from-content: 1
